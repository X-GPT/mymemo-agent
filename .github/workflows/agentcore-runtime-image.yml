name: AgentCore Runtime image

on:
  push:
    branches:
      - main
    paths:
      - apps/agentcore-canary-runtime/**
      - apps/agentcore-canary-dispatch/**
      - apps/agent-worker/**
      - packages/agent-db/**
      - packages/live-text/**
      - apps/*/package.json
      - packages/*/package.json
      - package.json
      - bun.lock
      - .dockerignore
      - scripts/smoke/agentcore-canary-runtime-image-check.sh
      - .github/workflows/agentcore-runtime-image.yml
  pull_request:
    paths:
      - apps/agentcore-canary-runtime/**
      - apps/agentcore-canary-dispatch/**
      - apps/agent-worker/**
      - packages/agent-db/**
      - packages/live-text/**
      - apps/*/package.json
      - packages/*/package.json
      - package.json
      - bun.lock
      - .dockerignore
      - scripts/smoke/agentcore-canary-runtime-image-check.sh
      - .github/workflows/agentcore-runtime-image.yml

concurrency:
  group: agentcore-runtime-image-${{ github.ref }}
  cancel-in-progress: true

jobs:
  image-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - name: Build ARM64 AgentCore Runtime image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/agentcore-canary-runtime/Dockerfile
          platforms: linux/arm64
          load: true
          tags: mymemo-agentcore-canary-runtime:image-check
      - name: Verify request-oriented image offline
        run: scripts/smoke/agentcore-canary-runtime-image-check.sh mymemo-agentcore-canary-runtime:image-check
