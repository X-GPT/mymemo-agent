{
	"name": "mymemo-docs",
	"version": "0.1.0",
	"type": "module",
	"scripts": {
		"test": "bun test",
		"build": "bun run build.ts",
		"format": "biome format --write",
		"lint": "biome lint --write",
		"check": "biome check --write"
	},
	"dependencies": {
		"citty": "^0.2.2"
	},
	"devDependencies": {
		"@types/bun": "^1.3.11"
	}
}
